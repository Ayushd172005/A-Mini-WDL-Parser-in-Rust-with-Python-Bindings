[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "wdl-lite"
version = "0.1.0"
description = "Minimal WDL parser with Python bindings via PyO3"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
